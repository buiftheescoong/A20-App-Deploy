/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable if you need to connect to external image domains
  // images: {
  //   domains: ['your-supabase-project.supabase.co'],
  // },
};

module.exports = nextConfig;
